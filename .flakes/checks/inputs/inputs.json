{
  "inputs": {
    "nixpkgs": {
      "description": "Nixpkgs package set - provides packages and lib utilities",
      "source": "shared",
      "required": true,
      "updatePolicy": "auto"
    },
    "crane": {
      "description": "Crane - Rust build tool for Nix, used for clippy and test checks",
      "source": "shared",
      "required": true,
      "updatePolicy": "auto"
    },
    "project-root": {
      "description": "Project root directory (non-flake path input) - used for accessing project files",
      "source": "project",
      "required": true,
      "updatePolicy": "never"
    }
  }
}

