{
  "inputs": {
    "nixpkgs": {
      "description": "Nixpkgs package set - provides formatting tools (nixpkgs-fmt, cargo, rustfmt)",
      "source": "shared",
      "required": true,
      "updatePolicy": "auto"
    }
  }
}

